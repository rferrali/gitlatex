push <- function(folder) {
  print("pushing")
  # warn if not on main
  # warn if not on latest commit
  # warn if some files in remote are not in local (except LaTeX build files)
  # warn if some files in remote are different from local and more recent
  # push
  # remove everything in remote
  # copy everything from local
  # copy shared tables and figures library
}
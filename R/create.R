create <- function(name, remote, local) {
  print("creating")
  # error
  ## name exists
  # TODO
  # add remote to .env
  ## default remote is remote_default/name
  # add project to lockfile
  ## default local is local_default/name
}